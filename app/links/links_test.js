'use strict';

describe('myApp.links module', function() {

  beforeEach(module('myApp.links'));

  describe('links controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var linksCtrl = $controller('LinksCtrl');
      expect(linksCtrl).toBeDefined();
    }));

  });
});